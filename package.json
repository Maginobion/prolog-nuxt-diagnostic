{
  "private": true,
  "scripts": {
    "build": "nuxt build; touch CMakeLists.txt; yum -y install cmake3-gui.x86_64;git clone https://github.com/SWI-Prolog/swipl-devel.git;cd swipl-devel;git submodule update --init;mkdir build;cd build; cmake -DCMAKE_INSTALL_PREFIX=$HOME -G Ninja ..;ninja;ctest -j 4;ninja install;",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "nuxt": "3.0.0"
  },
  "dependencies": {
    "@types/child-process-promise": "^2.2.2",
    "child-process-promise": "^2.2.1"
  }
}

