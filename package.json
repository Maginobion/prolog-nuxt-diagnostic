{
  "private": true,
  "scripts": {
    "build": "nuxt build; sudo apt-get install software-properties-common; yum install ppa:swi-prolog/stable;yum install swi-prolog",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "nuxt": "3.0.0"
  },
  "dependencies": {
    "@types/child-process-promise": "^2.2.2",
    "child-process-promise": "^2.2.1"
  }
}

