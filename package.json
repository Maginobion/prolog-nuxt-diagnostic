{
  "private": true,
  "scripts": {
    "build": "nuxt build; git clone https://github.com/SWI-Prolog/swipl-devel.git; ./bootstrap;make;make install;cd swipl-devel; git submodule update --init;cd swipl-devel;mkdir build;cd build;cmake -DCMAKE_INSTALL_PREFIX=/usr/local ..;make;ctest -j 4;sudo make install",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "nuxt": "3.0.0"
  },
  "dependencies": {
    "@types/child-process-promise": "^2.2.2",
    "child-process-promise": "^2.2.1"
  }
}

